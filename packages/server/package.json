{
  "name": "@passkey-kit/server",
  "version": "1.0.0",
  "description": "Server-side WebAuthn passkey verification with challenge-response and argon2 password hashing",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": ["dist"],
  "scripts": {
    "build": "tsc",
    "prepublishOnly": "npm run build"
  },
  "keywords": ["webauthn", "passkey", "fido2", "authentication", "argon2"],
  "license": "MIT",
  "dependencies": {
    "@simplewebauthn/server": "^13.1.1",
    "argon2": "^0.41.1"
  },
  "peerDependencies": {
    "express": "^4.0.0 || ^5.0.0"
  },
  "peerDependenciesMeta": {
    "express": { "optional": true }
  },
  "devDependencies": {
    "@types/express": "^5.0.0",
    "@types/node": "^22.0.0",
    "typescript": "^5.7.0"
  }
}
